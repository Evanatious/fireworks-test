from setuptools import setup, find_packages

setup(
    name='fireworks',
    version='0.1',
    description='A general HTTP load-testing and benchmarking library for sending HTTP requests and measuring performance.',
    author='Evan Gao',
    packages=find_packages(),
)